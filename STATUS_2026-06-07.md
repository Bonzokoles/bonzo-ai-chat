# STATUS 2026-06-07

## Cel tego dokumentu

Zebrać w jednym miejscu aktualny stan `The_Buch`, żeby dalsza praca nad sklepem nie mieszała się z porządkami runtime, agentów i wiedzy.

## Co jest aktywne

- lokalna aplikacja `The_Buch` działa na `http://127.0.0.1:4149`
- frontend:
  - `index.html`
  - `app.js`
- backend:
  - `backend/app/main.py`
- terminal:
  - `xterm.js` + websocket `/ws/terminal`
  - zostawiony aktywny i nietknięty funkcjonalnie
- routing modeli:
  - `9router` jako warstwa routingu lokalnego
  - modele direct przez OpenRouter nadal dostępne

## Co zostało uporządkowane

- runtime `The_Buch` został oddzielony od starych śmieci i launcherów
- stare artefakty zostały przeniesione do `NOT_in_USE`
- dokumentacja lokalna została wyrównana do realnego runtime
- port runtime został ustalony na `4149`
- branding i opisy zostały uproszczone do `EastWood Ops` / `EASTWOOD`

## Czego nie ruszać

- `The_brain/`
  - zostaje, bo zawiera bazę filmową, filozofię, czarny humor i prywatne materiały
- `knowledge_base/`
  - zostaje wyłączone z tych prac
  - Claude może tam dodawać wiedzę

## Prompt i zachowanie BUCH

Do systemu zostało wpisane, że:

- użytkownik mówi do bota `BUCH`
- bot działa lokalnie w `S:\BONZO_I_DO_SHELL_SHOP`
- korzysta z plików, konfiguracji, wiedzy i terminala
- ma być wyluzowany, pewny siebie, konkretny
- ma trzymać poker face
- ma unikać:
  - sztucznego zachwytu
  - korpo tonu
  - marketingowej papki
  - wywyższania się
- ma działać jak realny operator CLI, a nie tylko odpowiadać teoretycznie

## Modele i routing

Dodane i ustawione:

- główne:
  - `x-ai/grok-4.3`
  - `z-ai/glm-5.1`
  - `moonshotai/kimi-k2.6`
- dodatkowe / direct:
  - `minimax/minimax-m3`
  - `minimax/minimax-m2.7`
  - `deepseek/deepseek-v4-pro`
  - `tencent/hy3-preview`
- rerank config:
  - `RERANK_PROVIDER=cohere`
  - `RERANK_MODEL=rerank-v4.0-fast`

`9router` został sprawdzony i działa lokalnie.

## Podział subsystemów

### 1. BUCH

Rola:

- główny lokalny operator
- chat
- terminal
- pliki
- konfiguracja
- diagnostyka

### 2. PINKY_one

Rola:

- prywatna pomoc operacyjna
- sklep
- Shopify
- panel
- checklisty
- FAQ

### 3. the_deal_BOYS

Rola:

- sourcing
- marże
- B2B
- lejki
- Polaczki
- przyszły orchestrator

## Profile zadaniowe

Aktywne profile:

- `default`
- `shop_help`
- `shopify`
- `private_help`
- `agent_ops`
- `research`
- `deal_ops`
- `content`
- `debug`

## Rozdział wiedzy

### PINKY_one

Zakres:

- operacje
- produkty i usługi Yutro
- ton prywatnego chata
- finanse ecommerce PL
- Shopify i panel FAQ

Własne endpointy:

- `/api/private-help/info`
- `/api/private-help/context`
- `/api/private-help/chat`

Wdrożone:

- osobny loader: `backend/app/private_help_loader.py`
- osobny fallback leksykalny dla kontekstu z plików `PINKY_one`
- `private_help` zwraca:
  - `kb_scope`
  - `kb_sources`
  - konkretne pliki użyte do odpowiedzi

### the_deal_BOYS

Własne endpointy:

- `/api/deal-ops/info`
- `/api/deal-ops/context`
- `/api/deal-ops/chat`

Wdrożone:

- osobny loader: `backend/app/deal_ops_loader.py`
- worker suggestions dla `Polaczek_*`
- osobny tor strategiczny poza zwykłym webhookiem BUCH

## Co widać teraz w UI

Przy odpowiedzi asystenta widać:

- `profile`
- `model`
- `provider`
- `kb`
- `kb-src`
- `style`
- `files`
- `tools`

To pozwala zobaczyć:

- jaki profil zadziałał
- jaki model odpowiedział
- z jakiego toru wiedzy poszła odpowiedź
- z których plików poszedł kontekst

## Embeddings i wiedza

Zrobione:

- `knowledge_mood` jest aktywnym źródłem wiedzy
- indeks wiedzy nie mieli już:
  - `node_modules`
  - `venv`
  - `dist`
  - `build`
  - `.git`
  - `__pycache__`

Stan praktyczny:

- dla `PINKY_one` najpewniejszy jest obecnie fallback leksykalny
- to jest świadome i działa stabilnie
- nie blokuje już prywatnej pomocy na stanie embeddingów

## Co było sprawdzone

- `py_compile` dla backendu
- `node --check` dla `app.js`
- lokalny backend restartowany i sprawdzany na `4149`
- testowane:
  - `/api/health`
  - `/api/private-help/info`
  - `/api/private-help/context`
  - `/api/private-help/chat`
  - `/api/deal-ops/info`
  - `/api/deal-ops/context`
  - `/api/deal-ops/chat`
  - `/api/chat` z profilem `private_help`

## Aktualny stan do dalszej pracy

Warstwa `The_Buch` jest wystarczająco uporządkowana, żeby przerzucić uwagę na sklep.

To znaczy:

- runtime działa
- terminal działa
- rozdział subsystemów jest zrobiony
- prompt systemowy jest ustawiony
- modele są ustawione
- `PINKY_one` i `the_deal_BOYS` są od siebie oddzielone
- UI pokazuje, z czego odpowiedź realnie skorzystała

## Czego jeszcze nie robić teraz

- nie budować jeszcze wielkiego orchestratora
- nie mieszać `PINKY_one` z `the_deal_BOYS`
- nie ruszać `The_brain`
- nie ruszać `knowledge_base`

## Następny etap

Następny etap to praca nad sklepem, już bez wracania do bazowego cleanupu `The_Buch`.
